import BookingDetail from '../features/bookings/BookingDetail';

function Booking() {
  return <BookingDetail />;
}

// A page should not fetch data or have side effects, cleaner pages
export default Booking;
